"""One module per existing Typer command group, registered on the
root app by `qbit_ops.cli.app`. See `_shared.py` for cross-group
mutation-flow helpers (`run_mutation`, `exit_if_no_targeted_matches`).
"""

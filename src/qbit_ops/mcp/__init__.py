"""Experimental MCP surface over `qbit_core`.

Personal spike, read-only. Not a supported product surface: see
`docs/MCP.md` for what it is for and how to remove it.

Nothing here holds domain logic. Every verdict, classification and
threshold comes from `qbit_core`; this package only shapes bounded
answers for an agent. That is what makes it deletable in one commit.
"""
